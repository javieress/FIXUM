<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partials/head') %>
        <title>
            <%= title %>
        </title>
</head>

<body>
    <%- include('../'+navBar) %>
    <a href="/edit/AssetTypes" style="color: white" class="btn btn-dark mx-5 mt-5 ">
        <div class="mx-2 my-0">
            <p class="my-0"><i class="fa-solid fa-arrow-left-long"></i> Tipos de Activos</p>
        </div>
    </a>
        <div class="container h-100 mt-5">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col-xl-9">
                    <div class="card" style="border-radius: 15px;">
                        <div class="card-header p-4">
                            <h3>Editar tipo de activo</h3>
                        </div>
                        <div class="card-body">
                            <form method="post" action="/register/asset-type-update" class="row g-3 needs-validation" novalidate>
                                <div class="row align-items-center pt-4 pb-3">
                                    <div class="col-md-3 ps-5">
                                        <label for="validationCustom01" class="form-label">Nombre del tipo de activo fijo: </label>
                                    </div>
                                    <div class="col-md-9 pe-5">
                                        <input type="hidden" class="form-control form-control-lg" name="new-asset-type-id" value="<%= assetType[0].dataValues.id%>" id="validationCustom01" required/>
                                        <input id='new-asset-type-name' type="text" class="form-control form-control-lg" name='new-asset-type-name' value="<%= assetType[0].dataValues.assetType %>" maxlength="50" id="validationCustom01" required/>
                                        <div class="invalid-feedback">
                                            Debes rellenar este campo con el nombre del nuevo tipo de activo.
                                        </div>
                                    </div>
                                </div>
                                <hr class="mx-n3">
                                <div class="px-5 py-4">
                                    <button type="submit" class="btn btn-dark btn-lg">Actualizar</button>
                                </div>
                                <% if(message == 'El Tipo de Activo se editó con éxito'){ %>
                                    <div class="alert alert-success text-center"><%=message%></div>
                                 <% }else if (message != ''){ %>
                                    <div class="alert alert-danger text-center"><%=message%></div>
                                 <% } %>
                            </form>
                        </div>
                    </div>

                </div>
            </div>
        </div>
</body>
<%- include('../partials/footer') %>

</html>